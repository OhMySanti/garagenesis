package org;

import org.vehicles.*;
import org.terminal.DisplayVehicles;
import org.terminal.Menus;

import static org.terminal.DisplayVehicles.testMe;
import static org.terminal.Menus.mainView;

public class Main {
    public static void main(String[] args) {
        testMe();
        mainView();

    }
}